<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">
                <img alt="Brand" src="{{ asset('assets/img/1479093439_ilustracoes_04-10.svg') }}" width="20px">
            </a>
        </div>
        <ul class="nav navbar-nav">
            <li class="active"><a href="{{ path('homepage') }}">{{ 'Home'|trans }}</a></li>
            <li><a href="#">{{ 'My books'|trans }}</a></li>
        </ul>
        <form class="navbar-form navbar-left" action="{{ path('homepage') }}">
            <div class="input-group">
                <input type="text" class="form-control" placeholder="Search for...">
                <span class="input-group-btn">
                    <button class="btn btn-default" type="button">{{ 'Go'|trans }}</button>
                </span>
            </div><!-- /input-group -->
        </form>
        {% if is_granted('ROLE_USER') %}
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                       aria-expanded="false">{{ app.user.name }} <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="#">{{ 'Logout'|trans }}</a></li>
                    </ul>
                </li>
            </ul>
        {% else %}
            <a href="{{ path('homepage') }}" class="btn btn-default navbar-btn navbar-right">{{ 'Login'|trans }}</a>
            <a href="{{ path('register_user') }}"
               class="btn btn-default navbar-btn navbar-right">{{ 'Register'|trans }}</a>
        {% endif %}
    </div>
</nav>